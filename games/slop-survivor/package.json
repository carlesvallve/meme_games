{
  "name": "@sttg/slop-survivor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "deploy": "vite build && cd dist && vercel --prod --yes",
    "verify": "node scripts/verify-runtime.mjs",
    "iterate": "node scripts/iterate-client.js --url http://localhost:3000"
  },
  "dependencies": {
    "@sttg/game-base": "workspace:*",
    "@sttg/audio": "workspace:*"
  }
}
