// Template scaffolding — part of the @sttg/game-base contract.
// Not used by the dungeon-crawler example (which uses zustand via store.ts),
// but kept so new games scaffolded from this template have a ready-made state container.
export { GameState, gameState } from '@sttg/game-base';
